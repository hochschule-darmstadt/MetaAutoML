apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: mljar
  name: mljar
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: mljar
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: mljar
    spec:
      containers:
      - env:
        - name: GRPC_SERVER_PORT
          value: "50053"
        image: alexhda/<PUT PATH HERE>
        imagePullPolicy: ""
        name: mljar
        ports:
        - containerPort: 1433
        resources:
          limits:
            cpu: "2"
            memory: "2097152e3"
        volumeMounts:
        - name: datasets
          mountPath: /app/app-data/datasets
        - name: output-mljar
          mountPath: /app/app-data/output
      restartPolicy: Always
      serviceAccountName: ""
      volumes:
      - name: datasets
        persistentVolumeClaim:
          claimName: omamldata-claim
      - name: output-mljar
        persistentVolumeClaim:
          claimName: omamldata-claim
