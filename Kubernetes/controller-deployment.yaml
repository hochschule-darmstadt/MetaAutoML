apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: controller
  name: controller
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: controller
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: controller
    spec:
      volumes:
      - name: omamldata-pvc
        persistentVolumeClaim:
          claimName: omamldata-claim
      containers:
      - env:
        volumeMounts:
        - name: omamldata-pvc
          mountPath: /omaml
        image: alexhda/omaml-controller
        imagePullPolicy: ""
        name: controller
        ports:
        - containerPort: 1433
        resources:
          limits:
            cpu: "2"
            memory: "2097152e3"
      restartPolicy: Always
      serviceAccountName: ""
status: {}
