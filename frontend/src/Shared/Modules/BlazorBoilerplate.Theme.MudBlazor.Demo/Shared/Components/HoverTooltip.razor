<style>
    .position {
        bottom: 61%;
        /*bottom:62.5%;*/
        right: 2%;
    }
    .position-recentTrainings{
        bottom:59%;
        right:19.6%;
    }

    .position-recentDatasets-upload {
        bottom: 59%;
        right: 44.1%;
    }
    .position-recentDatasets {
        bottom: 59%;
        right: 66.9%;
    }
    .position-recentDatasets-more{
        bottom: 59%;
        right: 38.5%
    }
    .position-recentTrainings-more {
        bottom: 59%;
        right: 0.5%
    }
    .tooltip .tooltiptext {
        visibility: hidden;
        display: block;
        text-align: center;
        position: absolute;
        /*  bottom: 63.5%;*/
    }

        .tooltip .tooltiptext::after {
            content: " ";
            position: absolute;
            top: 100%;
            left: 50%;
            margin-left: -5px;
            border-width: 5px;
            border-style: solid;
            border-color: grey transparent transparent transparent;
        }


    .tooltip:hover .tooltiptext {
        visibility: visible;
        background-color: grey;
        opacity: 50%;
    }

</style>
<MudText Typo="Typo.body1" class="tooltip position private-scope">
    @ChildContent

    <MudText Class="@Position" Style="opacity: 100%!important; color:white!important;font-size:11pt;">
        @if (lines != null)
            for (int i = 0; i < lines.Length; i++)
            {
                @lines[i];
                <br />
            }
        @if (!string.IsNullOrEmpty(URL))
        {
            <MudLink Style="color:white!important" Href="@URL">
                more...
            </MudLink>
        }


    </MudText>
</MudText>

@code {
    [Parameter]
    public string Label { get; set; }
    [Parameter]
    public string URL { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }

    
    [Parameter]
    public string Position { get; set; }


    string[] lines;
    protected override void OnParametersSet()
    {
        lines = Label?.Split('\n');
    }

}

