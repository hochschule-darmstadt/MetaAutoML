@inherits DynamicComponentContainer

<footer class="drawer-footer" style="background-color: white">
    @foreach (var component in components)
    {
        @CreateDynamicComponent(component);
    }
</footer>
